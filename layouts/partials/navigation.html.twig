        {#- navigation menu ~#}
        {%- for entry in site.menus.main|sort_by_weight ~%}
        <a href="{{ url(entry.url) }}" data-weight="{{ entry.weight }}">{{ entry.name }}</a>
        {%- endfor -%}